fn main(){
    let implicit=true; //declaracion de tipos implicida - se infiere tipo por el sistema de inferencia de rust
    let explicit:bool=false;//declaracion explicita de una variable tipo,se recomienda usar esta 
    println!("Min {}",i64::min_value());
    println!("Max {}",i64::max_value());
}